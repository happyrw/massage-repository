/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "img.icons8.com", "th.bing.com", "images.pexels.com"
        ]
    }
};

export default nextConfig;
